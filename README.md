# Lab8-Starter
Jason Wang, Andrew Le

## How are graceful degradation and service workers related?
Service workers ensure that when user loses network access, the page will still work as it installs the content of the page when the user had access to network. Service workers implement graceful degradation by giving a controlled fallback path when the network or features fail.
